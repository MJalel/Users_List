import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Users_Details',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
